# B10 成本候選映射核對紀錄

核對日期：2026-09-11。唯讀盤點，未寫入或重新儲存原 Excel，也未產生成本副本。

## 結果

- 原檔 SHA-256 前後皆為 `f2c581dd308eaa2750b16532e976e940b7576b26809ad0ea92b0a6b35d5e2d2b`，檔案大小與修改時間亦一致。
- `詳細價目表` 依 F=`D×E` 公式辨識 399 列明細成本候選；另辨識 13 列小計並排除候選輸入。
- 明細中 H 已有數量 82 列、I 已有單價 82 列、H/I 皆已填 82 列、部分填寫 0 列、H/I 皆空白 317 列。
- 已附掛 423 列緊鄰文字／編碼／備註續行；每筆保留原項次、單位、正式 D 數量、H/I 舊值、J:L 既有公式及候選 H/I 位址。
- A:G 的唯讀內容指紋前後一致：`b3a015c843ba08b91133f499b1fca90f3f2d5e1016071af1bc09c0e737158984`。

## 映射原則

H:I 僅是結構上的候選輸入地址。輸出中的 `proposed_input_values` 一律為 `null`；既有 H/I 值只作原檔證據保留。所有 399 列仍標記來源待確認，後續須補廠商、日期、原檔／頁次、稅別與有效性，才能判斷是否可寫入。

## 限制

- 未提供廠商報價來源，因此不推定、補值或宣稱任何成本已獲來源確認。
- 空白 H/I 不等於 0；E 欄正式報價單價未被拿來代替成本。
- 間接工程、稅額、暫列與特殊工項即使符合公式型態，仍需人工核對來源及可寫性。
- 本次未啟動 Excel 原生重算或外觀渲染；只做公式、值與列結構的唯讀抽取。

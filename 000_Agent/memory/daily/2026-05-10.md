# 2026-05-10 日誌

## 今天完成的事

### pro-kit 07：跨裝置同步引導助手

完整跑完雷小蒙的跨裝置同步引導助手，建立 AI 分身可攜化基建。

**Mac 端**
- `settings.json`、`commands/`、`keybindings.json` 從 `~/.claude/` 搬到 Dropbox 母體，建立 symlink
- `skills` symlink 已早先由 pro-kit 01 建好
- 更新 `.gitignore`：細粒度排除 `.claude/` 本機狀態，保留需同步的核心檔案
- GitHub 私有 repo（tuchenghong/Tu-agent）推送完成

**Windows 端**
- Dropbox 安裝在 D: 槽（`D:\Dropbox\Tu-agent`）
- `skills`、`commands` 建立 Junction 指向 Dropbox
- `settings.json` 因 Windows 檔案 symlink 限制，改用複製版（手動更新）
- Claude Code 開啟後 skills 正常載入（`/morning` 等指令可用）

**自動化**
- `sync-health.sh` 體檢腳本：cron 每週五 09:00 自動執行，log 寫到 `000_Agent/logs/`
- `claude-startup.bat` 放進 Windows 啟動資料夾：偵測 symlink，已設定則靜默退出

**踩坑紀錄**
- Windows PS1 腳本含中文 here-string → 編碼錯誤，改為全英文版本
- `claude-startup.bat` 用捷徑執行時 `%~dp0` 抓到捷徑路徑而非原始位置 → 改用直接複製檔案
- Windows 檔案 symlink 需要特殊權限，`mklink` 建立失敗 → 改用 `Copy-Item` 複製
- Dropbox 在 D: 槽，路徑偵測需明確加入 `D:\Dropbox\Tu-agent`

## 最終架構

```
~/.claude/ (Mac)
├── settings.json  → Dropbox/.claude/settings.json
├── commands/      → Dropbox/.claude/commands/
├── keybindings.json → Dropbox/.claude/keybindings.json
└── skills/        → Dropbox/000_Agent/skills/

Windows .claude/
├── skills/        Junction → D:\Dropbox\Tu-agent\000_Agent\skills
├── commands/      Junction → D:\Dropbox\Tu-agent\.claude\commands
└── settings.json  複製版（需手動更新）
```

## 未完成 / 後續注意

- Windows `settings.json` 不自動同步，Mac 改設定後需手動 `Copy-Item` 更新
- 每週五確認 `000_Agent/logs/sync-health.log` 有無異常
